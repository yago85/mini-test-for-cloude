# Test Task

The project has two files: `data.js` and `logic.js`. Something is wrong — running `node logic.js` does not print `SUCCESS`.

1. Find and fix all the bugs across both files so the output is correct.
2. Run `node logic.js` to verify the output.
3. Expected output (both lines must appear):
   ```
   Order total: $1214.78
   SUCCESS
   ```
4. Once you see both lines, your task is complete. Do not make any further changes.
